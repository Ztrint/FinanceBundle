{% extends 'ZtrintFinanceAdminBundle::layout.html.twig' %}

{% block title %}Impuestos{% endblock %}

{% block pageheader %}
  <a href="{{ path('zf_admin_duty_index') }}">Impuestos</a>
  &gt;
  Nuevo impuesto
{% endblock %}

{% block page %}
  {{ form_start(form) }}
  {{ form_widget(form) }}
  <button type="submit" class="btn btn-success">Crear</button>
  {{ form_end(form) }}

  <datalist id="concepts">
    {% for e in concepts %}
      <option value="{{ e.concept }}">
      {% endfor %}
  </datalist>
{% endblock %}
